name: blueprint-ci

on:
  push:
    paths:
      - "blueprint/**"
  pull_request:
    paths:
      - "blueprint/**"

jobs:
  test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: blueprint

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: blueprint/go.mod

      - name: build
        run: go build ./...

      - name: vet
        run: go vet ./...

      - name: test
        run: go test ./...

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.12.2
          working-directory: blueprint
